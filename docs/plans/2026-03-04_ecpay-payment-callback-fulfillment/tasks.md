# Tasks: 綠界付款回推履約與通知時機調整

- Date: 2026-03-04
- Feature: 綠界付款回推履約與通知時機調整

## **Task 1: 法幣訂單持久化與建單流程調整**

對應 `R1.1`, `R1.2`，核心目標是把法幣下單改成「建立待付款訂單」而非「立即履約」。

- 1. [x] 新增法幣訂單資料模型與持久化層
  - 1.1 [x] 新增 Flyway migration（新增法幣訂單表與必要索引/狀態欄位）
  - 1.2 [x] 新增 repository/domain 並註冊至 Dagger module
- 1. [x] 調整 `FiatOrderService` 建單流程
  - 1.1 [x] 取號成功後保存訂單為待付款狀態
  - 1.2 [x] 移除建單當下履約呼叫，保留買家私訊內容（訂單編號+超商代碼）

## **Task 2: 綠界回推接收與付款完成觸發履約**

對應 `R2.1`, `R2.2`，核心目標是接收綠界推送並可靠驅動付款後履約。

- 2. [x] 新增回推處理服務
  - 2.1 [x] 實作回推 payload 解析/解密與訂單對應
  - 2.2 [x] 實作付款完成（含等價狀態）判斷與冪等處理
- 2. [x] 新增 callback 入口與啟停生命週期
  - 2.1 [x] 新增最小 HTTP callback receiver（符合綠界回推格式）
  - 2.2 [x] 於 bot 啟動/關閉流程掛載與釋放 receiver

## **Task 3: 管理員通知時機調整與測試補強**

對應 `R3.1`, `R3.2`，核心目標是把護航型商品通知時機從建單改為付款完成。

- 3. [x] 調整通知觸發點
  - 3.1 [x] 移除法幣建單即管理員通知
  - 3.2 [x] 付款完成且為護航型商品時才發送管理員私訊
- 3. [ ] 補上測試與驗證
  - 3.1 [x] 單元測試：建單、回推判斷、冪等履約、通知時機
  - 3.2 [ ] 整合測試：回推→狀態更新→履約/通知鏈路（E2E 不可行時以 integration 強化）
