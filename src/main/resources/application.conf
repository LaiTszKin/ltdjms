# Compatibility shim only.
#
# Runtime canonical packaged defaults live in `application.properties`.
# `EnvironmentConfig` no longer loads this file as a live defaults source.
#
# Keep this file free of configuration keys so it cannot drift into a
# second schema by accident.
